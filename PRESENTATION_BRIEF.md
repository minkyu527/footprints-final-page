# Footprints — 수업 발표 웹사이트 브리핑

> 이 파일을 기반으로 발표용 웹사이트를 만들어 주세요.
> 목적: **인간-AI 상호작용 수업** 과제 발표 / 청중: 교수 + 동기들

---

## 1. 프로젝트 한 줄 소개

> **"폰만 들고 다니면 추억이 쌓인다"**
> 발자국(Footprints)은 친구·지인과의 만남과 연락을 자동으로 기록·분석해 관계를 잊지 않게 도와주고 이를 시각화해주는 AI 관계 비서 앱입니다.

---

## 2. 해결하는 문제 (Why)

- 바쁜 일상에서 가까운 사람들과의 만남이 기억에서 흐릿해짐
- 수동으로 만남을 기록하는 건 귀찮아서 결국 안 하게 됨
- "이 친구 마지막으로 언제 봤더라?" 가 생각나지 않음
- 카카오톡 연락은 하지만 직접 만남은 없는 관계의 온도 파악 불가

---

## 3. 핵심 기능 (What)

### 3-1. 발자국 자동 기록
- **구글 캘린더 연동**: 일정 제목에서 친구 이름 자동 감지 → 만남 발자국 자동 생성
- **GPS 체크인**: 앱(EXPO) 실행 시 현재 위치를 카카오 지도 API로 역지오코딩
- **날씨 자동 태깅**: 만남 기록 시 Open-Meteo API로 날씨(☀️🌧️⛅❄️) + 기온 자동 저장

### 3-2. 카카오톡 연락 발자국
- macOS Accessibility API(osascript)로 카카오톡 채팅방 목록 읽기
- **대화 내용 미수집** — 채팅방 이름 + 마지막 연락 날짜만 추적
- 친구 프로필에 "🗓️ 마지막 만남" / "💬 마지막 연락" 분리 표시
- 인사이트: "만났는데 연락이 없는 친구" / "연락은 하지만 못 만난 친구"

### 3-3. 관계별자리 (Constellation Map)
- 친구들을 별자리처럼 시각화 — 친밀도·만남 빈도로 거리/크기 결정
- 같은 그룹(모임)에 속한 친구들은 점선으로 연결
- Three.js 기반 인터랙티브 3D 별자리

### 3-4. AI 매니저 브리핑
- 채팅 UI 형태의 AI 비서 — 매일 아침 관계 리포트 자동 생성
- "이번 주 N회 만남 · 가장 많이 만난 OOO"
- "멀어진 인연들" — 30일 이상 만남·연락 없는 친구 목록 + 구글 캘린더 빈 날짜 추천
- 픽셀 아트 캐릭터 애니메이션

### 3-5. 통계 & 인사이트
- 날씨별 만남 패턴 (도넛 차트)
- 날씨 × 친구 교차 분석 ("비 오는 날 자주 만나는 사람")
- 만남 빈도 바 차트, 친구별 타임라인

---

## 4. 기술 스택 (How)

| 영역 | 기술 |
|---|---|
| 프론트엔드 | Next.js 14 (App Router) + TypeScript |
| 스타일 | 다크 패널 계층 · Pretendard 폰트 · 불투명 카드 |
| 데이터베이스 | Supabase (PostgreSQL) |
| 인증 | NextAuth (구글 OAuth) |
| 지도 | 카카오 Maps API (역지오코딩) |
| 날씨 | Open-Meteo API (무료, API 키 불필요) |
| 3D 시각화 | Three.js (관계별자리) |
| 카카오 연락 | macOS Accessibility API (osascript) |
| Phase 2 모바일 | Expo (React Native) + expo-location |

---

## 5. 시스템 아키텍처

```
[사용자]
   │
   ├── 구글 캘린더 OAuth ──→ /api/sync ──→ 만남 자동 생성
   ├── GPS 체크인 ──────────→ /api/checkin ──→ 위치 기록
   ├── 카카오 크롤링 ────────→ /api/kakao-crawl ──→ 연락 발자국
   └── 수동 입력 ───────────→ /api/meetings ──→ 발자국 저장
                                    │
                              [Supabase DB]
                           friends / meetings /
                          contact_footprints /
                           calendar_events
                                    │
                        /api/briefing ──→ AI 매니저 브리핑
```

---

## 6. 데이터 모델 요약

```
friends        — 친구 목록 (이름, 친밀도, 마지막 연락일)
meetings       — 만남 발자국 (날짜, 장소, 날씨, GPS 좌표)
groups         — 모임 그룹 (동아리, 학교, 직장 등)
group_members  — 친구 ↔ 그룹 다대다
contact_footprints — 카카오 연락 발자국 (채팅방, 마지막 연락)
calendar_events    — 구글 캘린더 원본 이벤트
```

---

## 7. 화면 구성 (3열 데스크탑 레이아웃)

```
┌─────────────────┬──────────────────┬─────────────────┐
│  AI 매니저      │  관계별자리      │  만남 발자국    │
│  (채팅 UI)      │  (별자리 지도)   │  (타임라인)     │
│                 │  친밀도 차트     │                 │
│  - 관계 리포트  │  만남 통계       │  연락 발자국    │
│  - 멀어진 인연  │  날씨 도넛 차트  │  (카카오 히트맵)│
│  - 픽셀 캐릭터  │                  │                 │
└─────────────────┴──────────────────┴─────────────────┘
```

---

---

## 10. 발표 웹사이트에 꼭 담아야 할 내용

1. **히어로 섹션**: "발자국" 타이틀 + 핵심 가치 문장 + 웹 스크린샷
2. **문제 정의**: 관계가 흐릿해지는 공감 포인트 (감성적 글)
3. **기능 소개**: 카드 3~4개 — 새로고침(원터치만 하면 된다는 것 어필) / 카카오 크롤링(자정에 진행하는) / 관계별자리2D/ 3D / 친구연결(카카오톡 이름을 따와서 실제 웹에 연결) / 통계 /  (짧은 동영상 자동재생으로 나열, 그리고 재생바도 있어야해 특정 부분으로 넘기기 가능하도록)
5. **기술 스택 배지**: Next.js / Supabase / Kakao API / Three.js / Open-Meteo


---

## 11. 디자인 가이드 (기존 앱과 통일)

- **배경색**: `#0D0D0F` (거의 검정)
- **패널색**: `#141416` (진한 다크)
- **액센트**: `#00FFC2` (민트 그린) — 버튼, 강조 텍스트
- **텍스트**: `#E8E8E8` (밝은 회색)
- **뮤트 텍스트**: `#6B7280`
- **폰트**: Pretendard Variable (한글 최적화)
- **금지**: 글래스모피즘(backdrop-filter blur) 사용 금지
- **카드**: 불투명 다크 배경 + 얇은 테두리 (`rgba(255,255,255,0.1)`)
